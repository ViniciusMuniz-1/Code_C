#include <stdio.h>

unsigned int retornar_menor(int array[]){

}

int main(){
    int n;
    scanf("%d", &n);
    int array[n];
    for(int i = 0; i < n; i++){
        scanf("%d", &array[i]);
    }
    retornar_menor(array);
}